<%= render 'layouts/errors' %>

<%= form_tag @url, remote: false do  %>
	<%= label_tag(:name, "Name") %>
	<%= text_field_tag(:name) %>
	<br>
	<%= label_tag(:password, "Password") %>
	<%= password_field_tag(:password) %>
	<br>
	<br>
	<%= submit_tag("Submit", class: 'btn btn-primary') %>
<% end %>